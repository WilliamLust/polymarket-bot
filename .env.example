# Polymarket Trading Bot — Environment Variables
# Copy to .env and fill in values. NEVER commit .env.

# Trading (leave empty for backtesting-only mode)
POLYGON_WALLET_PRIVATE_KEY=
FUNDER_ADDRESS=

# APIs
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Ollama (local)
OLLAMA_BASE_URL=http://localhost:11434

# X/Twitter (xurl is already configured)
# XURL_AUTH_TOKEN=  # already in xurl config
